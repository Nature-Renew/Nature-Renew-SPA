const environment = {
    TABLE_NAME: process.env.TABLE_NAME,
    REGION: process.env.REGION,
    API_GATEWAY_ENDPOINT: process.env.API_GATEWAY_ENDPOINT,
    AMPLIFY_FRONTEND: process.env.AMPLIFY_FRONTEND,
};

export default environment;

